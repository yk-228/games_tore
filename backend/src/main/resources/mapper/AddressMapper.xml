<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.gamestore.dao.AddressMapper">
    <select id="findById" resultType="com.example.gamestore.entity.Address">
        SELECT * FROM t_address WHERE id = #{id}
    </select>
</mapper>
